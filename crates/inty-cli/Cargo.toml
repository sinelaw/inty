[package]
name = "inty-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Command-line interface for inty (type checker + LSP server)"

[[bin]]
name = "inty"
path = "src/main.rs"

[dependencies]
inty = { path = "../inty" }
inty-lsp = { path = "../inty-lsp" }
